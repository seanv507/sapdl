library(arules)
plot(cars)
?read.table
library(arules)
xtrain = read_table('offline_challenge/xtrain_obfuscated.txt', header=FALSE,          col.names=c('text'))
library(arules)
xtrain = read.table('offline_challenge/xtrain_obfuscated.txt', header=FALSE,          col.names=c('text'))
ytrain = read.table('offline_challenge/ytrain.txt',header=FALSE, col.names=c('label'))
xytrain=cbind(xtrain,ytrain)
labels =pd.read.tablecsv('labels.txt',header=None, col.names=c('label','title'), sep='\t')
library(arules)
xtrain = read.table('offline_challenge/xtrain_obfuscated.txt', header=FALSE,          col.names=c('text'))
ytrain = read.table('offline_challenge/ytrain.txt',header=FALSE, col.names=c('label'))
xytrain=cbind(xtrain,ytrain)
labels =pd.read.table('labels.txt',header=None, col.names=c('label','title'), sep='\t')
library(arules)
xtrain = read.table('offline_challenge/xtrain_obfuscated.txt', header=FALSE,          col.names=c('text'))
ytrain = read.table('offline_challenge/ytrain.txt',header=FALSE, col.names=c('label'))
xytrain=cbind(xtrain,ytrain)
labels =read.table('labels.txt',header=None, col.names=c('label','title'), sep='\t')
library(arules)
xtrain = read.table('offline_challenge/xtrain_obfuscated.txt', header=FALSE,          col.names=c('text'))
ytrain = read.table('offline_challenge/ytrain.txt',header=FALSE, col.names=c('label'))
xytrain=cbind(xtrain,ytrain)
labels =read.table('labels.txt',header=FALSE, col.names=c('label','title'), sep='\t')
